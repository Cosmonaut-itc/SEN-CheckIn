'use client';

import React from 'react';

/**
 * Temporary placeholder for the employee import wizard.
 *
 * @returns Empty client boundary until Task 8 fills the full flow
 */
export function ImportClient(): React.ReactElement {
	return <div className="sr-only" />;
}
