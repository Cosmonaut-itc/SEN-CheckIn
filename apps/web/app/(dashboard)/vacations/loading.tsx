import { VacationsSkeleton } from '@/components/skeletons';

/**
 * Loading component for the Vacations page.
 *
 * @returns Vacations loading skeleton
 */
export default function VacationsLoading() {
	return <VacationsSkeleton />;
}
