import { config } from "@sen-checkin/eslint-config/nextjs";

/**
 * ESLint configuration for the Next.js web admin portal.
 * Extends the shared Next.js ESLint configuration.
 */
export default config;
